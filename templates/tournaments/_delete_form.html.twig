<form method="post" action="{{ path('tournaments_delete', {'trId': tournament.trId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament.trId) }}">
    <button class="btn">Delete</button>
</form>
