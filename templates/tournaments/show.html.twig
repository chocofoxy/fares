{% extends 'base.html.twig' %}

{% block title %}Tournaments{% endblock %}

{% block body %}
    <h1>Tournaments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>TrId</th>
                <td>{{ tournament.trId }}</td>
            </tr>
            <tr>
                <th>TrCover</th>
                <td>{{ tournament.trCover }}</td>
            </tr>
            <tr>
                <th>GameId</th>
                <td>{{ tournament.gameId }}</td>
            </tr>
            <tr>
                <th>Team1Id</th>
                <td>{{ tournament.team1Id }}</td>
            </tr>
            <tr>
                <th>Team2Id</th>
                <td>{{ tournament.team2Id }}</td>
            </tr>
            <tr>
                <th>Team3Id</th>
                <td>{{ tournament.team3Id }}</td>
            </tr>
            <tr>
                <th>Team4Id</th>
                <td>{{ tournament.team4Id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournaments_index') }}">back to list</a>

    <a href="{{ path('tournaments_edit', {'trId': tournament.trId}) }}">edit</a>

    {{ include('tournaments/_delete_form.html.twig') }}
{% endblock %}
