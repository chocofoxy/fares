<form method="post" action="{{ path('tournaments_intr_delete', {'trId': tournaments_intr.trId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournaments_intr.trId) }}">
    <button class="btn">Delete</button>
</form>
